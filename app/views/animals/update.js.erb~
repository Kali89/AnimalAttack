$(function() {
	var docHeight = $(document).height();

	$("body").append("<div id='overlay'></div>")

	$("#overlay")
		.height(docHeight)
		.css( {
			'opacity' : 0.8,
			'position': 'absolute',
			'top': 0,
			'background-color': 'black',
			'width': '100%',
			'margin':'auto',
			'z-index': 5000,
			'color': 'yellow',
			'text-align':'center'
		})
		.html("<p>"+"<%= escape_javascript(@winningAnimal.name.to_s) %>"+"  had ranking: "+"<%= escape_javascript(@winningBefore.to_s) %>" +".</p>"+"<p>"+"<%= escape_javascript(@losingAnimal.name.to_s) %>"+"  had ranking: "+"<%= escape_javascript(@losingBefore.to_s) %>" +".</p>"+ "<p>"+"<%= escape_javascript(@winningAnimal.name.to_s) %>"+"  now has ranking: "+"<%= escape_javascript(@winningAnimal.rating.to_s) %>" +".</p>"+"<p>"+"<%= escape_javascript(@losingAnimal.name.to_s) %>"+"  now has ranking: "+"<%= escape_javascript(@losingAnimal.rating.to_s) %>" +".</p>");
});
setTimeout(function() {window.location.replace("/");},2500);
